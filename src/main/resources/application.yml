spring:
  datasource:
    url: jdbc:postgresql://cuenta-db:5432/cuenta_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    admin:
      auto-create: false
server:
  port: 8082
  error:
    include-stacktrace: never
